import 'package:my_oga_mechanic/imports.dart';

class MenuVehicleProfile extends StatelessWidget {
  const MenuVehicleProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('vehicle profile');
  }
}
